// src/app/admin/nfl-ratings/page.tsx
// Admin NFL Ledger: full controls (sync, seed, recalculate, manual entries).

import NflRatingsView from '@/components/NflRatingsView';

export default function NflRatingsAdminPage() {
  return <NflRatingsView admin />;
}
