// src/app/fbs/page.tsx
// Public read-only FBS power ratings + upcoming projections
// (linked from the NCAAF tab next to FCS).

import FbsRatingsView from '@/components/FbsRatingsView';

export default function FbsPublicPage() {
  return <FbsRatingsView />;
}
